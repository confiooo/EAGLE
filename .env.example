# Copy to .env and fill in (never commit .env).
# Required when config.json has "mode": "telegram". Not used for "web" mode.
# Create a bot with @BotFather and paste the token here.
TELEGRAM_BOT_TOKEN=your_bot_token_here
